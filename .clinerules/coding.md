1. Break down complex content into clear, scannable sections with descriptive headings (h1, h2, h3). Use bullet lists for enumerations — avoid dense paragraphs.
2. Apply KISS (Keep It Simple, Stupid) principles. These are static legal pages — no interactivity, no frameworks, no build steps.
3. Keep responses and explanations concise and to the point — avoid unnecessary verbosity.
4. Understand the full problem context before providing solutions. When updating legal text, verify the corresponding app behaviour matches the description.
5. Use semantic HTML elements (`<h1>`–`<h6>`, `<p>`, `<ul>`, `<ol>`, `<strong>`, `<a>`). Do not use `<div>` for headings or navigation.
6. Generate meaningful, self-documenting class names where needed. Since CSS is embedded, minimise class usage — rely on element and descendant selectors for layout.
7. Reference the existing visual patterns (container max-width, border-left highlights, footer styles) when adding new pages or sections. Maintain visual consistency across all three pages.